# OpenAPI Export

Production FastAPI deployments should export the generated OpenAPI document here so the web app and external operators can pin API contracts. The local fixture server exposes the same route shapes but avoids runtime dependencies.

