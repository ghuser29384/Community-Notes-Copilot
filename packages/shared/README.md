# Shared Contracts

Shared schemas for API clients, eval fixtures, and future FastAPI/Next TypeScript generation. The current dependency-light runtime serializes the same record names that should become Pydantic v2 models and OpenAPI schemas in production.

