services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cn_ops
    ports:
      - "5432:5432"
    volumes:
      - cn_ops_pg:/var/lib/postgresql/data

volumes:
  cn_ops_pg:

